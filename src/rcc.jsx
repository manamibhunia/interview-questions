import React, { Component, PropTypes} from 'react';

export default class ScopeMain extends Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (<div>MyComponent</div>);
  }
}

ScopeMain.propTypes = {
};
